protocol bgp AS212034_FRA from ix_peer
{
         neighbor 2a0a:6040:ee::1:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS212034_ADL from ix_peer
{
         neighbor 2a0a:6040:ee::4:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS212034_FRE from ix_peer
{
         neighbor 2a0a:6040:ee::5:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS212034_SNG from ix_peer
{
         neighbor 2a0a:6040:ee::7:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS212034_PAR from ix_peer
{
         neighbor 2a0a:6040:ee::9:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}

protocol bgp AS212034_HNG from ix_peer
{
         neighbor 2a0a:6040:ee::11:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS212034_SEO from ix_peer
{
         neighbor 2a0a:6040:ee::16:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS212034_MON from ix_peer
{
         neighbor 2a0a:6040:ee::17:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}

protocol bgp AS212034_NEW from ix_peer
{
         neighbor 2a0a:6040:ee::21:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS212034_TOK from ix_peer
{
         neighbor 2a0a:6040:ee::23:2120:34:1 as 212034;
         description  "212034 - SCLRNET.com";
         passive on;
         import filter
         {
         if peer_in(212034) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(212034) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}


