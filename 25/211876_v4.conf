protocol bgp AS211876_FRA from ix_peer
{
         neighbor 172.30.1.13 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
		if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_AUK from ix_peer
{
         neighbor 172.30.3.11 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_FRE from ix_peer
{
         neighbor 172.30.5.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_VAN from ix_peer
{
         neighbor 172.30.6.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_SNG from ix_peer
{
         neighbor 172.30.7.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_SHA from ix_peer
{
         neighbor 172.30.8.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_ROM from ix_peer
{
         neighbor 172.30.10.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_HNG from ix_peer
{
         neighbor 172.30.11.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_AMS from ix_peer
{
         neighbor 172.30.13.11 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
		if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_KAN from ix_peer
{
         neighbor 172.30.19.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
		if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_SEO from ix_peer
{
         neighbor 172.30.16.11 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_MOS from ix_peer
{
         neighbor 172.30.18.12 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_MIA from ix_peer
{
         neighbor 172.30.20.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_NYC from ix_peer
{
         neighbor 172.30.21.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_TOK from ix_peer
{
         neighbor 172.30.23.10 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
