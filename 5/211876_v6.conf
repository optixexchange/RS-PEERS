protocol bgp AS211876_FRA from ix_peer
{
         neighbor 2a0a:6040:ee::1:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
		if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_AUK from ix_peer
{
         neighbor 2a0a:6040:ee::3:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_VAN from ix_peer
{
         neighbor 2a0a:6040:ee::6:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_SNG from ix_peer
{
         neighbor 2a0a:6040:ee::7:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_SHA from ix_peer
{
         neighbor 2a0a:6040:ee::8:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_ROM from ix_peer
{
         neighbor 2a0a:6040:ee::10:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_HNG from ix_peer
{
         neighbor 2a0a:6040:ee::11:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_AMS from ix_peer
{
         neighbor 2a0a:6040:ee::13:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
		if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_KAN from ix_peer
{
         neighbor 2a0a:6040:ee::19:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
		if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_SEO from ix_peer
{
         neighbor 2a0a:6040:ee::16:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_MOS from ix_peer
{
         neighbor 2a0a:6040:ee::18:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_MIA from ix_peer
{
         neighbor 2a0a:6040:ee::20:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_NYC from ix_peer
{
         neighbor 2a0a:6040:ee::21:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}
protocol bgp AS211876_TOK from ix_peer
{
         neighbor 2a0a:6040:ee::24:2118:76:1 as 211876;
         description  "211876 - fixmix Technologies Ltd";
         passive on;
         import filter
         {
                if peer_in(211876) = true && net ~ PREFIXES then accept;
                reject;
         };

         export filter
         {
                if optix_prefix() then bgp_path.add(OPTIX_ASN);
                if peer_out(211876) = true  && ((65530,2000)) !~ bgp_community then accept;
         };
}